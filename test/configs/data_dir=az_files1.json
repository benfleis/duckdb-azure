{
  "description": "Run against Azure Blob Store with env DATA_DIR=az://files1/test_data",
  "on_init": "LOAD '{BUILD_DIR}/extension/azure/azure.duckdb_extension'; CREATE OR REPLACE SECRET ( TYPE azure, PROVIDER credential_chain, CHAIN 'env', ACCOUNT_NAME 'duckfiles' );",
  "data_dir": "az://files1/test_data"
}
